<!--index.wxml-->

<view class="container">

  <view class="main">
    <view class="logo"><image src="/images/logo.png" /></view>
    <view class="name" data-text="猜电视">猜电视</view>
    <view class="slogan">那些年全家人坐在一起看电视的快乐时光，你还记得吗？</view>
  </view>

  <view class="menu">
    <button open-type="getUserInfo" bindgetuserinfo="onGetUserInfo" class="button submit {{loaded ? '' : 'disabled'}}" wx:if="{{actived != 1}}">开始游戏 <small>授权</small></button>
    <button bindtap="onStart" class="button submit {{actived == 1 ? '' : 'disabled'}}" wx:if="{{actived == 1}}">开始游戏</button>
    <button bindtap="onRank" class="button {{actived == 1 ? '' : 'disabled'}}">排行榜</button>
    <button open-type="share" class="button">邀请好友 PK</button>
  </view>
</view>

<dialog id="dialogStart">
  <view class="dialog-text">
    <em class="content">你已经答对了 <strong>{{duration}}</strong> 道题，现在你可以</em>
    <dfn class="choice">A：【使用复活卡】继续上次的题目 <var>你有 <strong>{{cards}}</strong> 张复活卡</var></dfn>
    <dfn class="choice">B：选择【重新开始】再来一次 <var>你的得分将会被清零</var></dfn>
  </view>
  <view class="dialog-action">
    <button bindtap="onContinue" class="button submit {{cards > 0 ? '' : 'disabled'}}">使用复活卡</button>
    <button bindtap="onRestart" class="button">重新开始</button>
    <button open-type="share" bindtap="onShare" class="button" data-action="save">求助好友 <small>复活卡 +1</small></button>
  </view>
</dialog>

<dialog id="dialogSave">
  <view class="dialog-text">
    <image src="{{fromClientAvatarUrl}}" class="avatar" />
    <em class="content">你的好友 <strong>{{fromClientNick}}</strong>
        <dfn wx:if="{{fromOpenGId}}"> 来自群
          【<open-data type="groupName" open-gid="{{fromOpenGId}}"></open-data>】
        </dfn>
        向你求助 ，希望你能帮他激活一张复活卡</em>
  </view>
  <view class="dialog-action">
    <button bindtap="onSaved" class="button submit">帮Ta激活</button>
    <button bindtap="onAbort" class="button">放弃</button>
  </view>
</dialog>
